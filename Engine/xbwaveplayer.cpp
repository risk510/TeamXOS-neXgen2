// Wave Player
#include "xbwaveplayer.h"

//LPDIRECTSOUND8            m_pDSound;          // DirectSound object
//LPDIRECTSOUNDBUFFER8      m_p3DBuffer;        // 3D buffer from audiopath

void temp()
{
	//m_p3DBuffer->
}
//CXBSound *m_DPadUPSnd = new CXBSound(); 

/*void PlaySound(CXBSound *pSound) 
{ 
	if (pSound) { 
		if (pSound->m_pDSoundBuffer) { 
		pSound->m_pDSoundBuffer->SetCurrentPosition(0); 
		pSound->Play(); 
		} 
	} 
} */

/*if (m_DPadUPSnd!=NULL && FAILED(m_DPadUPSnd->Create("C:\Audio\MainAudio\Global Main MenuBack3ver2.wav")) ) 
{ 
	delete m_DPadUPSnd; 
	m_DPadUPSnd = NULL; 
}*/